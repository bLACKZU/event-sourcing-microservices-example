# Default values for postgresql (overridden by parent chart)
# All PostgreSQL configuration comes from the parent chart's values

# These are just defaults - the parent umbrella chart overrides all of these
dbsecurityContext: {}
dbenv: []
existingSecret: ""
