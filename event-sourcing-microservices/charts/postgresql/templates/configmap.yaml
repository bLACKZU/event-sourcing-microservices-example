apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgresql.fullname" . }}-configmap
labels:
  {{- include "postgresql.labels" . | nindent 4 }}
data:
  pre-stop.sh: |-
  {{ .Files.Get "scripts/pre-stop.sh" | indent 4 }}
    
    