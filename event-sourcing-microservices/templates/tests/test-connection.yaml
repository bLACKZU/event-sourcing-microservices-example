apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-sourcing-microservices.fullname" . }}-test-connection"
  labels:
    {{- include "event-sourcing-microservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-sourcing-microservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
